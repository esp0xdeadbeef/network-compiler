- state=solved | target=emitter-provenance-volatile-source-lastmodified | evidence=2026-06-02 Wave 1 Agent 1 all-example compiler artifact sweep under `/tmp/network-compiler-wave1-agent1-20260602T223315Z` compiled 23 locked `network-labs/examples/*/intent.nix` inputs with identical `.meta.compiler.gitRev = aac256dabf4312672a3a29805370105116cc9a50`, `gitDirty = true`, and `sourceNarHash = sha256-a6XjEJu9M9qDi5ZPRRcrMIyZLiDh2i2CyEhORM5NMdA=`, but emitted `sourceLastModified` values `1780439560` and `1780439629`; focused rerun later emitted `1780439653` for the same source hash. `flake.nix` now emits `sourceLastModified = "unknown"` when no stable source revision is available, while preserving `sourceNarHash`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-emitter-provenance-repo-boundary.sh` passes. Post-fix proof under `/tmp/network-compiler-wave1-agent1-postfix-20260602T223844Z/reports/provenance.tsv` shows all 23 locked example outputs use one source hash and `sourceLastModified = "unknown"`. | reason=FS-100 provenance metadata must identify the emitter and deterministic source content; dirty path-flake source-copy modification time is volatile output metadata and is not a stable locked tool-chain identifier.

- state=solved | target=emitter-provenance-alternate-worktree | evidence=2026-06-02 Wave 3 parent check ran `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-emitter-provenance-repo-boundary.sh` from alternate-named worktree `/tmp/network-compiler-fs100-work`. The pre-fix wrapper could only identify the emitter when the Git toplevel basename was `network-compiler`; the wrapper now also accepts a Git remote identity matching `network-compiler`, so valid worktrees with different directory names still emit the compiler revision. Focused proof passed after the fix. | expected=Compiler output provenance shall record the compiler emitter revision for any valid compiler Git worktree, independent of checkout directory basename, while still rejecting foreign caller repositories. | owner=network-compiler | reason=FS-100 provenance must identify the emitter repository by repository identity, not local directory naming.

- state=solved | target=emitter-provenance-foreign-cwd | evidence=2026-06-02 Wave 2 Agent F generated compiler output from caller cwd `/home/deadbeef/github/network-codex-agent` and found `.meta.compiler.gitRev` incorrectly recorded caller revision `3f47e2ac04c92c64be906b888e9daabc3d159d63`. `flake.nix` now records compiler Git state only when the current Git toplevel is the `network-compiler` emitter repo; foreign-cwd path-flake runs emit `gitRev = "unknown"` plus source hash and `sourceLastModified = "unknown"` instead of a false caller revision or volatile source timestamp. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-emitter-provenance-repo-boundary.sh`. Current foreign-cwd artifact proof: `/tmp/network-compiler-wave1-agent1-postfix-20260602T223844Z/reports/foreign-cwd-provenance.tsv`. | reason=Compiler output provenance is compiler-owned construction metadata. Downstream stages, GAMP evidence, and SAT reports must not attribute compiler artifacts to the orchestrating repository.

- state=solved | target=overlay-termination-core-client-lan-underlay | evidence=2026-05-31 HAT evidence showed `core-nebula` must be a DHCP/SLAAC client-LAN member without a renderer-materialized P2P adapter to upstream. The compiler had a stale core-stage adjacency invariant that still forced every core node to have an explicit topology link to an upstream-selector, which produced `E_CORE_STAGE_ADJACENCY_REQUIRED` when the s-sigma intent/inventory removed the old `core-nebula <-> upstream` link. `lib/correctness/topology/core-stage-adjacency.nix` now accepts an overlay termination core with explicit `transport.overlays[].underlayAccess` to an access node as canonical upstream reachability while keeping the normal explicit core-to-upstream-selector rule for WAN/ISP cores. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-canonical-access-attachment.sh` proves the positive shape has no topology link touching `core-overlay`, still emits the underlay/control path through access/downstream/policy/upstream/WAN core, and rejects missing underlay egress to WAN. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/check.sh` passes. | reason=The compiler owns semantic topology validity. Overlay daemon underlay reachability may be modeled as client-LAN access without creating a durable P2P topology edge; downstream renderers must consume the explicit underlay access contract rather than materializing an invented core-upstream adapter.

- state=solved | target=overlay-underlay-host-like-core-connectivity | evidence=2026-05-24 full `s-router` rebuild failed before NFM/renderer with compiler `E_TOPO_DISCONNECTED` for `esp.clab` after the s-sigma intent changed `core-nebula` underlay from a p2p link to a host-like client tenant attachment. The topology validator now treats an explicit `transport.overlays[].underlayAccess` tenant attachment on the terminating core as semantic connectivity to the selected access node without adding a topology link, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-canonical-access-attachment.sh` passes while asserting there is no `core-overlay <-> access-client` p2p link and the underlay traffic path still compiles as `core-overlay -> access-client -> downstream -> policy -> upstream -> core-wan`. | reason=The compiler owns semantic topology validity. A modeled overlay daemon underlay client attachment must be accepted as intent semantics without forcing a physical p2p topology edge that downstream renderers would materialize incorrectly.

- state=solved | target=multi-site-overlay-egress-policy-path | evidence=2026-05-16 `bash tests/test-overlay-ingress-policy-reference.sh`, `bash tests/test-canonical-traffic-paths.sh`, and `bash tests/test-uplink-name-ambiguity.sh` pass; spot compile of network-labs lab-s-sigma/s-router-test-three-site now emits hetz allow-hostile-overlay-egress-to-wan as hetz-router-nebula-core -> hetz-router-upstream -> hetz-router-policy -> hetz-router-upstream -> hetz-router-core with corePathNodes [ hetz-router-nebula-core hetz-router-core ]; duplicate site uplink names fail with E_UPLINK_NAME_NOT_UNIQUE; same-core external-to-external egress fails with E_TRAFFIC_PATH_EXTERNAL_CORE_LOOP | reason=intent must select WAN egress by explicit external.uplinks and the compiler must reject ambiguous site uplink names or any overlay ingress path that would return to the same core for WAN egress; downstream stages must not collapse overlay ingress into a WAN shortcut

- state=solved | target=overlay-underlay-access-selector | evidence=2026-05-22 live s-sigma compiler artifacts showed Nebula underlay/control relations were compiled as direct core/upstream/core paths, while `git blame -L 721,803 README.md` identified commit `499df3ad` as the existing documentation that already required an underlay/client-side access attachment. The compiler now preserves `transport.overlays[].underlayAccess`, allows only that explicit overlay termination core <-> access exception in topology validation, emits underlay/control traffic paths through the selected access node, and rejects an underlayAccess tenant that has no modeled egress relation to the target WAN external with `E_OVERLAY_UNDERLAY_ACCESS_WAN_EGRESS_REQUIRED`. `tests/test-overlay-ingress-policy-reference.sh` now uses `underlayAccess = { kind = "tenant"; name = "client"; }` in its synthetic `esp.hetz` fixture, matching `network-labs/labs/lab-s-sigma/s-router-test-three-site/intent.nix`; `tests/test-overlay-address-pools.sh` now declares the selected `lan` tenant's `nebula` underlay egress to WAN instead of relying on the overlay external's egress relation. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-canonical-access-attachment.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-ingress-policy-reference.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-address-pools.sh` pass locally, and a local compile of the s-sigma lab emits `core-nebula -> access-client -> downstream -> policy -> upstream -> WAN core` for the NixOS/CLAB underlay legs. | reason=The compiler owns overlay underlay semantics and preconditions. It must not derive the underlay access attachment from payload tenants such as hostile, and downstream NFM must consume the emitted path and prove the concrete routes do not collide or self-loop.

- state=solved | target=wildcard-target-traffic-path-alternatives | evidence=2026-06-02 read-only compiler artifact audit of locked `network-labs` examples found `to = "any"` relations in `ipv6-pd-downstream-delegation`, `single-wan*`, `single-wan-with-nebula-any-to-any-fw`, and `tri-site-s-router-overlay-egress` compile with `destination = "any"` but only one arbitrary destination access node in `nodePath` and `nodePathAlternatives`. GAMP lower-layer docs were updated under `FS-180-HDS-010-SDS-010-SMS-010` before CMC. `lib/stages/traffic-paths.nix` now expands only literal wildcard destinations across all compiler-known access nodes, with core-pair construction split into `lib/stages/traffic-paths/core-pairs.nix` to keep the path builder under the LOC guard. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-wildcard-target-traffic-paths.sh` passes and proves `destination = "any"` is preserved while `nodePathAlternatives` includes `access-admin`, `access-client`, and `access-mgmt` destination paths. | reason=The compiler owns relation-target normalization into canonical traffic traversal. A wildcard destination may remain `destination = "any"`, but the compiler must emit all eligible access destination alternatives so downstream NFM/CPM do not infer wildcard path coverage from names or one renderer-layout default.
